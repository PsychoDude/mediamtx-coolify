paths:
    all:
        source: rtmp://0.0.0.0:1935
        sourceOnDemand: yes

hls: yes
hlsVariant: lowLatency
# Enable controlling the server through the Control API.
api: no
# Address of the Control API listener.
apiAddress: :9997
# Enable publishing and reading streams with the RTSP protocol.
rtsp: yes
